require "insque"

config = YAML.load(File.open("#{Rails.root}/config/redis.yml"))[Rails.env]
Insque.redis = Redis.new(:host => config["host"], :port => config["port"])
Insque.sender = '<%= sender_name %>'
Insque.debug = true
